// action for successful login
export const loginSuccess = () => ({
    type: 'LOGIN_SUCCESS', // Action type indicating a successful login
  });
  
  // Aaction for logout
  export const logout = () => ({
    type: 'LOGOUT', // Action type indicating a logout
  });
  